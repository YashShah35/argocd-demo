apiVersion: v1
kind: Service
metadata:
  labels:
    app: hello-dotnet
  name: hello-dotnet
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 9090
  selector:
    app: hello-dotnet
  type: NodePort
