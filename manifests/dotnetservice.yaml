apiVersion: v1
kind: Service
metadata:
  labels:
    app: dotnetservice
  name: dotnetservice
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: dotnetservice
  type: NodePort
