apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hello-dotnet
  labels:
    run: hello-dotnet
spec:
  rules:
  - http:
      paths:
      - path: /app
        backend:
          serviceName: hello-dotnet
          servicePort: 80
