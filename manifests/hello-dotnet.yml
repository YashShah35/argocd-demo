apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    run: hello-dotnet
  name: hello-dotnet
  namespace: default
spec:
  replicas: 1
  ports:
  - port: 80
    protocol: TCP
    targetPort: 5001
  selector:
    app: hello-dotnet
  type: NodePort
  selector:
    matchLabels:
      run: hello-dotnet
  template:
    metadata:
      labels:
        run: hello-dotnet
    spec:
      containers:
      - name: mylocaldemo
        image: helloworldaspnetcore:latest
        imagePullPolicy: Never
